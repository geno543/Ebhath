/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'plus.unsplash.com', 'www.divinalaw.com', 'www.soas.ac.uk'],
  },
}

module.exports = nextConfig
